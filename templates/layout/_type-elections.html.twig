{% for typeElection in typeElections %}
{% if typeElection.name and typeElection.slug %}
    <li>
        <a class="dropdown-item" href="{{path('electionType', { slug: typeElection.slug })}}">{{ typeElection.name }}</a>
    </li>
    {% endif %}
{% endfor %}